[project]
name = "mcp-azure-apim"
version = "0.1.0"
description = "MCP server exposing Setlistfm API via Azure APIM"
authors = ["Benoit Moussaud <bmoussaud@microsoft.com>"]
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.12.0",
    "python-dotenv",
]

[tool.setuptools.packages.find]
where = ["."]